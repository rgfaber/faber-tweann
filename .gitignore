.eunit
*.o
*.beam
*.plt
erl_crash.dump
.concrete/DEV_MODE

# rebar 2.x
.rebar
rel/example_project
ebin/*.beam
deps

# rebar 3
.rebar3
_build/
_checkouts/
Mnesia.*/
doc/
rebar3.crashdump

# Lockfiles are not committed: loose constraints must stay loose so
# coordinated library updates are not blocked by a stale pin.
rebar.lock

# NIF build artifacts (built from source by priv/build-nifs.sh)
priv/*.so
priv/*.dylib
priv/*.d
native/*/target/
